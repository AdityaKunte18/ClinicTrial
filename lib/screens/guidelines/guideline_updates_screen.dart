import 'package:flutter/material.dart';

class GuidelineUpdatesScreen extends StatelessWidget {
  const GuidelineUpdatesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Guideline Updates')),
      body: const Center(child: Text('Pending / Accepted / Rejected — to be implemented')),
    );
  }
}
