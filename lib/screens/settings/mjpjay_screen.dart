import 'package:flutter/material.dart';

class MjpjayScreen extends StatelessWidget {
  const MjpjayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MJPJAY / PMJAY Data')),
      body: const Center(child: Text('Scheme packages & prerequisites — to be implemented')),
    );
  }
}
